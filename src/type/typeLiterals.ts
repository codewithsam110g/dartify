import * as ts from "ts-morph";
import { IRProperty, IRType, TypeKind } from "../ir/type";
import { parseType } from "./type";

export function handleTypeLiterals(node: ts.TypeLiteralNode, depth: number) :IRType {
  
}

function handleProperty():IRProperty { }